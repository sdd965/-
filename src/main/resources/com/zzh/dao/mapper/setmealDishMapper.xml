<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zzh.dao.mapper.SetmealDishMapper">
    <select id="selectIdsBySetmealId" resultType="java.lang.Long">
        select id from setmeal_dish
        <where>
            #{id} = setmeal_dish.setmeal_id
        </where>
    </select>
</mapper>